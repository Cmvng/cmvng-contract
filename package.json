{
  "name": "cmvng-contract",
  "version": "1.0.0",
  "scripts": {
    "deploy": "npx hardhat run deploy.js --network arcTestnet"
  },
  "devDependencies": {
    "hardhat": "^2.19.0",
    "@nomicfoundation/hardhat-toolbox": "^4.0.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.0.0"
  }
}
